{
  "frontend/src/App.tsx": "Main application component with TanStack Router, ThemeProvider for dark mode support, and Toaster component for toast notifications across all routes.",
  "frontend/src/components/Layout.tsx": "Root layout with header/footer; simplified to show Admin nav link for all authenticated users (removed role-check dependency), keeping the same visual design.",
  "frontend/src/components/FloatingWhatsApp.tsx": "Fixed-position WhatsApp button component that accepts optional productName prop for dynamic message generation.",
  "frontend/src/components/Hero.tsx": "Full-width hero banner component displaying generated hero image with overlay, tagline, and CTA button.",
  "frontend/src/components/CategoryShowcase.tsx": "Grid showcase of four wood categories with generated images and links to filtered collection page.",
  "frontend/src/components/FeaturedProducts.tsx": "Featured products section fetching from backend and displaying in responsive grid with ProductCard components.",
  "frontend/src/components/ProductCard.tsx": "Reusable product card component that converts `ImageId` (Uint8Array) to a blob URL using a local `imageIdToUrl` helper for display, with fallback to a generated image.",
  "frontend/src/components/ImageGallery.tsx": "Product image gallery with main image display, thumbnail navigation, and prev/next controls — updated to accept `ImageId[]` (Uint8Array) instead of `ExternalBlob[]`, converting each to a blob URL via `imageIdToUrl`.",
  "frontend/src/components/ProductForm.tsx": "Product add/edit form with `initialCategory` and `categoryLocked` props restored, using `useAddProduct`/`useUpdateProduct` hooks, image compression via single-argument `compressImage(file)`, upload progress indicator, and proper loading/error states.",
  "frontend/src/pages/Home.tsx": "Homepage with Hero, CategoryShowcase, FeaturedProducts, about preview section, and customization CTA.",
  "frontend/src/pages/Collection.tsx": "Public collection page using direct imperative `actor.listProducts` calls for paginated fetching, client-side category filtering on accumulated products, Load More button, and error/retry handling.",
  "frontend/src/pages/ProductDetail.tsx": "Product detail page with large image gallery, full description, wood type/finish info, and WhatsApp enquiry button — updated to pass `product.imageUrls` (typed as `ImageId[]`) directly to `ImageGallery` which now accepts that type.",
  "frontend/src/pages/Customize.tsx": "Customization request form that now saves to backend via `useSubmitCustomizationRequest` AND opens WhatsApp — on submit, the request is stored in the backend (visible in admin panel) and WhatsApp opens with all form fields pre-filled targeting +91 9828288383. Shows loading spinner during submission and success confirmation after.",
  "frontend/src/pages/About.tsx": "About Us page with brand story, workshop image, specializations showcase, and contact information.",
  "frontend/src/pages/Login.tsx": "Login page using Internet Identity — fixed `saveProfile` call to pass the correct `UserProfile` shape `{ name: string }` instead of the old `{ id, principal, isAdmin, role }` format that no longer matches the backend type. Removed the `Variant_admin` import.",
  "frontend/src/pages/Contact.tsx": "Contact page with form submission that saves to backend AND opens WhatsApp — after successful `submitContactForm` mutation (which returns a WhatsApp URL from the backend), the page opens WhatsApp in a new tab with pre-filled message details, shows a success banner with an \"Open WhatsApp again\" link, and displays a toast notification.",
  "frontend/src/pages/AdminPanel.tsx": "Admin panel that renders the sidebar and main content immediately upon authentication — removed the blocking `actorFetching` condition so the UI shell paints synchronously without waiting for backend initialization.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations — exports `useListProductsPage`, `useFeaturedProducts`/`useGetFeaturedProducts` (alias), `useGetProducts`, `useAnalyticsSummary`/`useGetAnalyticsSummary` (alias), and all CRUD, analytics, customization, contact, and admin role hooks with proper query invalidation.",
  "frontend/src/index.css": "Global OKLCH color system with added admin-specific CSS tokens (admin-bg, sidebar-bg, sidebar-border) for both light and dark modes.",
  "frontend/tailwind.config.js": "Extended Tailwind config with new admin panel color tokens (admin-bg, sidebar-bg, sidebar-border) mapped to CSS variables.",
  "frontend/src/components/BulkProductImport.tsx": "Bulk import component that fetches asset images as `Uint8Array` bytes via `fetchImageAsBytes` and passes them directly as `ImageId[]` in the `Product.imageUrls` field, fixing the `ExternalBlob` type mismatch.",
  "frontend/src/hooks/useActor.ts": "Actor initialization hook that creates an authenticated HttpAgent from the Internet Identity when logged in (passing only `agent` to createActorWithConfig, never `agentOptions` simultaneously), or an anonymous actor when unauthenticated. Removed the broken `_initializeAccessControlWithSecret` call that was causing actor initialization failures.",
  "frontend/src/utils/imageCompression.ts": "Canvas-based image compression utility that now explicitly returns `Uint8Array<ArrayBuffer>` (with a cast from `ArrayBufferLike`) to satisfy the `ExternalBlob.fromBytes` type signature.",
  "frontend/src/components/AdminDashboard.tsx": "Analytics dashboard with per-section error state and retry button — if the analytics query fails or times out, shows an inline error message with a Retry button scoped to the dashboard area only; sidebar and other sections remain unaffected.",
  "frontend/src/components/AdminProductsSection.tsx": "Product management admin section using direct imperative `actor.listProducts(BigInt(offset), BigInt(PAGE_SIZE))` calls for paginated fetching with Load More accumulation, error/retry states, and add/edit/delete dialogs via ProductForm.",
  "frontend/src/components/AdminCategoriesSection.tsx": "Categories section showing wood-type categories and per-category product lists — updated to use the new `ProductForm` props (`onSuccess`/`onCancel` instead of `onSubmit`/`isSubmitting`), and fixes the `imageIdToUrl` helper to cast `buffer as ArrayBuffer`."
}